
#include "y/utest/driver.hpp"

Y_UTEST_DECL(16)
{
    Y_UTEST(tag);
    Y_UTEST(source);
    Y_UTEST(fc);
    Y_UTEST(pattern);
    Y_UTEST(posix);
    Y_UTEST(rx);
    Y_UTEST(scanner);
    Y_UTEST(lexer);
    Y_UTEST(xnode);
    Y_UTEST(grammar);
    Y_UTEST(parser);
}
Y_UTEST_EXEC()
