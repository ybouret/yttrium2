Y_CreateTest(test-ink)
include_directories(../../mkl)
Y_LinkLibraries(test-ink y-ink y-jive y-jpeg png_static tiff y-mkl)

enable_testing()

Y_Message("Preparing ${Y_Test}")
set(VAR "${CMAKE_CURRENT_SOURCE_DIR}/../../../var")
set(IMG "${VAR}/lena.tiff")

Y_UTEST(zflux)
Y_UTEST(bitmap)
Y_UTEST(pixmap)
Y_UTEST(broker)
Y_UTEST(ops)
Y_UTEST(hist ${IMG})
Y_UTEST(io)
Y_UTEST(fmt)
Y_UTEST(diff)
Y_UTEST(filter ${IMG})
Y_UTEST(draw)
Y_UTEST(grad ${IMG})
Y_UTEST(blur ${IMG})
Y_UTEST(max  ${IMG})
Y_UTEST(edge ${IMG})
